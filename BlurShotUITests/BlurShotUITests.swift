//
//  BlurShotUITests.swift
//  BlurShotUITests
//
//  Created by Umar Haroon on 2/7/16.
//  Copyright © 2016 Shazia Haroon. All rights reserved.
//

import XCTest


class BlurShotUITests: XCTestCase {
        
    override func setUp() {
        super.setUp()
        
        // Put setup code here. This method is called before the invocation of each test method in the class.
        
        // In UI tests it is usually best to stop immediately when a failure occurs.
        continueAfterFailure = false
        // UI tests must launch the application that they test. Doing this in setup will make sure it happens for each test method.
        XCUIApplication().launch()

        // In UI tests it’s important to set the initial state - such as interface orientation - required for your tests before they run. The setUp method is a good place to do this.
    }
    
    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }
    
    func testExample() {
        // Use recording to get started writing UI tests.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        

        
//        let app = XCUIApplication()
//        setupSnapshot(app)
//        app.launch()
//        
//        let element = app.otherElements.containingType(.StaticText, identifier:"Press The Blue Button To Begin").childrenMatchingType(.Other).elementBoundByIndex(1).childrenMatchingType(.Other).element
//        element.tap()
//        snapshot("0Launch")
//        element.childrenMatchingType(.Other).elementBoundByIndex(2).tap()
//        
//        let tablesQuery = app.tables
//
//        tablesQuery.buttons["Camera Roll"].tap()
//        app.collectionViews.childrenMatchingType(.Cell).elementBoundByIndex(9).tap()
//        app.sliders["0%"].tap()
//        snapshot("1WhiteBlur")
//        element.tap()
//        app.images["DarkMode"].tap()
//        element.tap()
//        snapshot("2BlurWithDarkMode")
        
        

        
    
    }
    
}
